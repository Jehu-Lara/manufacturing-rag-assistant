# Environment
.env

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
venv/

# Ingestion output (generated artifact — regenerate with `python -m ingestion.run`)
ingestion/output/

# Retrieval index artifacts (generated — regenerate with `python -m retrieval.build_index`)
retrieval/output/

# Grounded-review calibration sweep runs (generated — regenerate with
# `python -m src.features.evaluation.floor_sweep`)
eval/calibration/

# Reserved: never commit copyrighted source PDFs. If a future phase adds one,
# it goes here and its URL only is documented in corpus/SOURCES.md.
corpus/copyrighted/

# OS
.DS_Store
Thumbs.db

# Subagent-driven-development scratch workspace (ledgers, briefs, review packages)
.superpowers/

# Tool cache artifacts, not project content
.firecrawl/

# Experiment-only indexes (audit bucket 5) — never served, never committed.
retrieval/output/experiments/
